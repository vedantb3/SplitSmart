# Ignore bundler config.
/.bundle

# Ignore all environment files (except templates).
/.env*
!/.env*.erb

# Ignore logfiles and tempfiles (combined rule).
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/db/*.sqlite3
/db/*.sqlite3-journal
/db/*.sqlite3-[0-9]*

# Ignore public assets and precompiled JS packs (combined rule).
/public/assets
/public/packs
/public/packs-test

# Ignore code coverage, specs temp files, and test artifacts.
/coverage/
/spec/tmp
*.orig
rerun.txt
pickle-email-*.html

# Ignore master key and potentially sensitive files.
/config/master.key
config/initializers/secret_token.rb  # Only if you're ok with secrets being uploaded

# Ignore dotenv and environment specific files (assuming version control of secrets is not desired).
.env
.env*.local

# Ignore environment normalization files, assuming they're checked in.
/.bundle
/vendor/bundle
Gemfile.lock
.ruby-version
.ruby-gemset

# Ignore rvm files (unless specifically needed).
.rvmrc

# Ignore Bower and node_modules (assuming these are managed with package managers).
/vendor/assets/bower_components
*.bowerrc
bower.json
node_modules/

# Ignore Pow environment settings and Byebug history.
.powenv
.byebug_history

# Ignore Yarn files (assuming Yarn is used for package management).
/yarn-error.log
yarn-debug.log*
.yarn-integrity